import { SearchPresentPipe } from './search-present.pipe';

describe('SearchPresentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPresentPipe();
    expect(pipe).toBeTruthy();
  });
});
